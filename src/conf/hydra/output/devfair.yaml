# @package hydra
run:
  dir: /checkpoint/sergioarnaud/habitat-llm/results/${hydra.job.name/${WANDB.name}
  subdir: ${hydra.job.num}_${hydra.job.override_dirname}
sweep:
  dir: /checkpoint/sergioarnaud/habitat-llm/results/${hydra.job.name/${WANDB.name}
  subdir: ${hydra.job.num}_${hydra.job.override_dirname}
