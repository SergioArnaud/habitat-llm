# @package hydra
run:
  dir: /Users/sergioarnaud/Documents/habitat-llm/results/${WANDB.name}
  subdir: ${hydra.job.num}_${hydra.job.override_dirname}
sweep:
  dir: /Users/sergioarnaud/Documents/habitat-llm/results/${WANDB.name}
  subdir: ${hydra.job.num}_${hydra.job.override_dirname}
